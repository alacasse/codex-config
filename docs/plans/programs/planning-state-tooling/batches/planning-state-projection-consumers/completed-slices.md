# Planning-State Projection Consumers Completed Slices

| Slice | Commit | Outcome | Audit references |
|---|---|---|---|
| 1. Route Batch Runway projection reports | `cc9d49c` | Batch Runway routes supported history/reporting questions through planning-state projection reporting before broad historical scans when policy permits, while keeping active pickup on `current` and `validate`. | Validation: consumer routing tests, manifest tests, planning-state `current`/`validate`, `git diff --check`; review: clean `runway_reviewer` result recorded in the runway ledger. |
| 2. Route Architecture Program projection reports | `7742e5e` | Architecture Program Runway routes supported program history/reporting questions through policy-compatible projection reports without letting projection output select batches, replace ledgers or selected dispatch packets, or close findings. | Validation: consumer routing tests, architecture runner protocol tests, manifest tests, planning-state `current`/`validate`, `git diff --check`; review: clean `runway_reviewer` result recorded in the runway ledger. |
| 3. Route Legacy Removal projection reports | `500cdd9` | Legacy Removal routes supported history/reporting questions through policy-compatible projection reports as planning-state context only, without changing legacy classification or evidence standards. | Validation: consumer routing tests, manifest tests, planning-state `current`/`validate`, `git diff --check`; review: clean `runway_reviewer` result recorded in the runway ledger. |
| 4. Align metadata and close consumer obligations | this commit | Closed PST-16 and PST-17 with aligned program state, pointer-first closeout evidence, completed-slice archive, and no next-batch selection. | Validation: final pytest command passed 17; planning-state `current`/`validate` passed with existing redirect warnings only; hard-coding scan reported only one pre-existing neutral phrase; `git diff --check` passed. Review: clean `runway_reviewer` result after stale-ledger wording fix. |
