# Completed Slices

| Slice | Commit | Outcome | Audit references |
|---|---|---|---|
| 1. Make pickup single-owned | `d97881b` | Batch Runway, Architecture Program Runway, and Legacy Removal now invoke Planning State Diagnostic-First Pickup as the single Layout v1 operational pickup Interface while retaining their own semantic decisions. | Validation: `python -m pytest tests/test_planning_state_consumer_projection_routing.py -q` passed with 12 tests; `python scripts/planning_state.py current --root docs/plans`; `python scripts/planning_state.py validate --root docs/plans`; `git diff --check`; review: clean. |
| 2. Re-separate layout and pickup | `80a0041` | Planning Artifacts owns placement, naming, active-state file shape, archives, and roots while Planning State owns pickup ordering, validation, target-policy checks, and projection routing. | Validation: `python -m pytest tests/test_planning_state_consumer_projection_routing.py -q` passed with 13 tests; `python scripts/planning_state.py current --root docs/plans`; `python scripts/planning_state.py validate --root docs/plans`; `git diff --check`; review: clean after focused phrase-wrap recovery. |
| 3. Clarify program/runway handoff | `8955641` | Architecture Program Runway owns program findings, selected dispatch, batch queue, and closeout reconciliation; Batch Runway owns concrete execution ledgers, validation/review routing, commits, and completed-slice archives. | Validation: `python -m pytest tests/test_planning_state_consumer_projection_routing.py -q` passed with 14 tests; `python scripts/planning_state.py current --root docs/plans`; `python scripts/planning_state.py validate --root docs/plans`; `git diff --check`; review: clean. |
| 4. Define discovery roles and closeout | this commit | Legacy Removal now requires an explicit discovery role before durable ledgers and gates selected dispatch/direct Batch Runway handoff on program-owner selection; Dead Surface Audit is evidence-only, and program state is reconciled to completed with no successor batch selected. | Validation: `python -m pytest tests/test_planning_state_consumer_projection_routing.py tests/test_codex_features_manifest.py -q` passed with 22 tests; `python scripts/planning_state.py current --root docs/plans`; `python scripts/planning_state.py validate --root docs/plans`; hard-coding check no matches; `git diff --check`; review: clean after selected-dispatch gate recovery. |
