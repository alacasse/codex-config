# Ledger Dispatch Rule Dedupe Completed Slices

Archived note: this was part of an erroneous pre-execution abandonment closeout.
CCFG-8 has been restored to queued state for `work-batch`.

No slices were executed.

The queued CCFG-8 dispatch/runway pair was closed as abandoned before execution.
Historical note only: CCFG-8 has since been restored as the queued batch for
`work-batch`.
