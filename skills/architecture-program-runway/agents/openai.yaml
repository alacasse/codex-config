interface:
  display_name: "Architecture Program Runway"
  short_description: "Same-batch program closeout support"
  default_prompt: "For agents: reconcile the just-completed batch behind $work-batch without planning or selecting successor work."
