interface:
  display_name: "Legacy Removal"
  short_description: "Scope safe legacy cleanup ledgers"
  default_prompt: "Use $legacy-removal to identify and scope legacy-removal work before planning implementation."
