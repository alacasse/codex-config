interface:
  display_name: "Legacy Removal"
  short_description: "Scope exceptional legacy residues"
  default_prompt: "For agents: scope evidence-backed legacy residues only when a command, review route, or explicit investigation needs it."
